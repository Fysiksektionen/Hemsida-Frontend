// Paths need to be relative site root (e.g. relative to http://localhost:3000) and not use trailing /.
export const adminRootPath: string = '/admin';
export const apiRootUrl: string = 'http://localhost:3007';
// Not used:
export const staticRootPath: string = '/static';

// Note that this file is overridden by docker when running in a container.
