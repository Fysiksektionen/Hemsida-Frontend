export const USE_MOCK_API = true;
export const API_VERBOSE = true;

// Only used by the mock api
export const MOCK_API_CALL_DELAY = 100; // ms

// Only used for non-mock apis
export const API_ROOT_URL = 'http://localhost:3002';
